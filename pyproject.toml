[project]
name = "gl-team-status"
version = "0.1.0"
description = "Interactive TUI for monitoring team MR review status on GitLab."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "textual>=8.0.0",
    "pfzy>=0.3.4",
]
